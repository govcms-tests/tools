# GovCMS dependency auditing tool

A simple tool for retrieving information about GovCMS's module dependencies.

## Usage

```bash
python3 -m venv
pip3 install -r requirements.txt
source venv/bin/activate
python audit.py
```
